#include <stdio.h>

int main()
{
  int p = 1;
  int q = 0;
  int i, j;

  for (i == 0; i < 100; ++i)
  {
    printf("%d \n", i);
    q += i;

    for (j == 0; j < 100; j++)

    {
      p = p * 2;
    }
  }

  printf("%d \n", p);
}
